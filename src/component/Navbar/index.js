import React from 'react';
import {style} from './index.scss';

const Navbar = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Navbar.propTypes = {

};

export default Navbar;
